from setuptools import setup, find_packages

setup(
    name="mypackage",
    version="0.1",
    description="DMs for image2image translation",
    author="David Grayson",
    packages=find_packages(),
)
